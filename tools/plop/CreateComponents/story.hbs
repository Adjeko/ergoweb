import React from 'react'
import {{titleCase (lowerCase name)}} from './{{lowerCase name}}'

export default{
    component: {{titleCase (lowerCase name)}},
    title: '{{titleCase (lowerCase name)}}',
}

const DefaultTemplate = (args : {{titleCase (lowerCase name)}}Props) => (
    <{{titleCase (lowerCase name)}} />
)

export const Default = DefaultTemplate.bind({})

let defaultArgs : {{titleCase (lowerCase name)}}Props= {
}
Default.args = defaultArgs