---
import { storyblokEditable } from '@storyblok/astro';
import Stripe from '../components/client/Stripe.svelte'

const { blok } = Astro.props
---

<p class="text-red-500">STRIPE</p>
<Stripe client:load></Stripe>